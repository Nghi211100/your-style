# Base URL of the Nest API. Use 127.0.0.1 (not localhost) if server-side fetch fails on Windows.
# Blog and other RSC pages call this during render — start the API (`apps/api`) before `next dev`.
API_URL=http://127.0.0.1:3001
